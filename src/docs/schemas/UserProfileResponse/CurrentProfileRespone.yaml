type: object
properties:
    message:
        type: string
        example: "Profile updated successfully"
    data:
        type: object
        properties:
            profile:
                $ref: "../UserProfile.yaml"
